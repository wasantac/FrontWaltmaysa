import React from 'react';
import Footer from './Footer';
import Header from './Header';

const Carrito = () => {
    return (
        <div>
            <Header></Header>
            <Footer></Footer>
        </div>
    );
}

export default Carrito;
